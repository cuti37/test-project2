  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">Wall</h3>
    </div>
    <div class="panel-body">
      <%= form_for @post, html: {id: "post-form"} do |f| %>
        <div class="form-group">
          <input type="text" id="input-tags" name="tags" placeholder="tags" tabindex="-1" value="" class="selectized" style="display: none;">
        </div>
        <div class="form-group">
          <%= f.text_field :title, placeholder: "title", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_area :content, placeholder: "content", class: "form-control"%>
        </div>
        <%= f.submit "Post", class:"btn btn-primary" %>
        <div class="pull-right">
        </div>
        <% end %>
    </div>
  </div>
